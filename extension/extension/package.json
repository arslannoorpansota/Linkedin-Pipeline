{
  "name": "linkedin-lead-extractor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node test/salesnav.test.mjs && node test/autopage.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.30",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}