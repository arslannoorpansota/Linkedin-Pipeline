# Daily Report — 2026-09-22

> Day: Tuesday
> All agent activity for this date is logged below, newest at the bottom.

## Summary
- Leads researched: 708 triaged from list view (162 kept, 546 skipped) — filter pass only, no profile pulls
- DMs written: 0
- Emails written: 0
- Follow-ups written: 0
- InMails written: 0
- Other activity (setup/assets/ops): 3 (extension repo imported; Sales Nav export filtered to ICP; 57 sheet rows stamped re-surfaced)

---

## ⚠️ CARRIED OVER — backlog not worked this session

`pipeline_health.py --all` at session start reported:

| Queue | Count | Note |
|---|---|---|
| Overdue (no reply, next touch past due) | 333 | by touch: T2 104, T2-accepted 119, PARK 52, T3 32, T4 26 |
| Undecided (status New, research done) | 47 | all r6 direct-client; **36 of 47 have no hook recorded** |
| Same person, >1 row | 1 | rows 742 / 1663 (Rob B.) — needs merge |
| Same company, >1 live contact | 6 | incl. Koda Health 2257/2473, Legion Health 2357/2405 |

The overdue queue was **not** worked today. Per CLAUDE.md §10 it precedes new leads;
next session should start there rather than on the 105 new leads below.

---

## Entries

### 2026-09-22 — Extension repo imported into portfolio

Moved `/home/faizan/extension` into `portfolio/extension/`. Copied the 41 git-tracked
files only (308K); the other ~52M was `node_modules`, a venv and `data/`, all gitignored.
History was not carried over — both commits were already pushed to
`github.com/MuhammadRafay7/extension`, verified `main == origin/main` with nothing
unpushed before deleting the original. Committed as `ca3f8e3` on `improvement`.

`enrich/.env` existed only in the source tree (gitignored) and was preserved to the new
location at mode 600. It turned out to be an **unfilled copy of `.env.example`** — both
`LI_AT` and `LI_JSESSIONID` are empty, so enrichment is blocked until cookies are added.
Nothing was lost in the move; they were never set.

Chrome extension built (`npm run build`, then `watch`, so `dist/` currently holds the
unminified dev build). `npm run typecheck` passes. `npm test` fails on a missing
`test/fixtures/leadsearch.json` — pre-existing, the fixture never existed in the source
repo either.

### 2026-09-22 — Sales Nav export filtered: 708 → 162

Extension collected 708 leads. The export is a **healthcare / mental-health founder
list**: ~370 US, 179 Hospitals & Health Care, 79 Mental Health Care, only 26 Software
Development. Nearly all Founder/CEO titles.

This does not match CLAUDE.md §6 Type A (Seed–Series B, AI-native, engineering hiring
signal). Confirmed with Faizan to filter against the **non-technical-founder / no-CTO**
ICP (`WEBSITE_GAP_TRIAGE.md`, `keep-non-technical-no-cto`) instead, which fits the data.
Geo per §6: skip IN/PK/CN/TH/UK only.

Judgement written to `extension/data/decisions.json` (one entry per row, with reason);
`extension/apply_filter.py` copies rows only, so names, URNs and encodings stay
byte-identical. `enrich/check_filter.py` exits 0 — all 708 accounted for.

| Dropped for | n |
|---|---|
| Clinical practice / services only, nothing to build | 228 |
| Not a founder/owner decision maker | 109 |
| Sells IT/RCM/staffing/consulting — vendor or competitor | 73 |
| Nonprofit / member org, no product | 53 |
| Technical founder, AI-native or YC-backed | 34 |
| Excluded geo (IN/PK/CN/TH/UK) | 22 |
| Is the technical leader (CTO/CPO) | 18 |
| Too new in role, no product signal | 8 |

A first rule pass mis-sorted several rows (Populate, Soulside, Medicia.ai flagged as
competitors on product vocabulary; 16 Bit and Peer AI kept despite technical founders).
Rules were reordered and tightened before the final run.

Caveats on the kept set: row 606 (Aegis AI) is a clean ICP match but auto-insurance, not
healthcare — kept because geo was the only filter beyond the ICP. Row 177 (meme media)
is a weak keep, product signal present but thin build case.

### 2026-09-22 — Dedupe: 57 of 162 already in the sheet, stamped re-surfaced

Deduped the 162 against col C (name) and col E (company) across all 3,396 sheet rows,
with normalisation for legal suffixes and punctuation.

- 46 matched by exact name
- 11 more matched by company
- **105 genuinely new**

**46 of the 57 had already been triaged and skipped** with reasoning recorded — technical
founder, own engineering team, vendor relationship. The list-view filter kept people the
pipeline had already rejected on deeper evidence, which is the expected failure mode of
filtering without the account page. Their prior verdicts stand.

Appended to AW on all 57 (append-only, existing text preserved with ` | ` separator):

> Re-surfaced 2026-09-22 by Sales Nav healthcare-founder export (list row N); dedupe
> matched this existing row, no new row created. Prior verdict stands.

Written via `values().batchUpdate` with `USER_ENTERED` in a 6-attempt retry loop.
57/57 confirmed on read-back, all prior text intact, **no status, rating or cadence
field touched**. No new rows created, so the 2366 block is untouched.

### 2026-09-22 — 105 new leads parked pending enrichment

The 105 have no hook, no website and no headcount — the CSV carries only Sales Nav list
view. Writing notes now would repeat the problem `pipeline_health` already flags on the
undecided queue (36 of 47 with no hook, against CLAUDE.md §9 "hooks must be verifiable").

Enrichment supplies the website, headcount and recent posts, and is the agreed proxy for
the founder gate's CTO test — `enrich/linkedin.py` fetches company website, staff count,
employee range and lead posts, but **not** the CXO list or employee titles, so the
"no CTO, no engineering team" question cannot be answered automatically. Faizan's call:
proxy it from enriched data, uncertain cases recorded as caveats rather than skips.

Blocked on empty `LI_AT` / `LI_JSESSIONID` in `extension/enrich/.env`. Nothing written
to the sheet for these 105.
